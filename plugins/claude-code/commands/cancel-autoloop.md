# /cancel-autoloop

Cancel the currently active autoloop.

## Usage

```
/autoloop:cancel-autoloop
```

This command stops the iterative loop immediately. Use when:
- You need to take a different approach
- The task requirements have changed
- You want to exit the loop manually

---

```bash
"$PLUGIN_DIR/scripts/cancel-loop.sh"
```
