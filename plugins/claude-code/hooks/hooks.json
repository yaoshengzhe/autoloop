{
  "description": "Autoloop stop hooks for autonomous iterative loops (v2 + v3)",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/stop.sh"
          },
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/scripts/stop-work.sh"
          }
        ]
      }
    ]
  }
}
